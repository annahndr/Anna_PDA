### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame

#need initialize method

  def checkforAce(card) #improve test name
    if card.value = 1
      return true
    else
      return false
    end
  end

#'dif' should be 'def'
  dif highest_card(card1 card2)
  #indenting is a bit off
  if card1.value > card2.value
    #there is no name method and should specify which card
    return card.name
  else
    #needs a return and a method
    card2
  end
end
#too many 'ends'
end

def self.cards_total(cards)
  total #needs to be assigned a value
  for card in cards
    total += card.value
    return "You have a total of" + total #needs #{}
  end
end

#missing 'end' for the class
```
